var should = require('should');

describe('calc', function() {
  describe('.add', function() {
    it('should return sum of 2 arguments', function() {
      Number(3).should.equal(3);
    });

    it('error', function() {
      Number(3).should.equal(3);
    });
  });
});
